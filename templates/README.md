# Report templates

Drop the student report templates here as zip archives. `students.html` links to:

- `report-template-latex.zip`
- `report-template-typst.zip`

If you name the files differently, update the two `.paper-links` blocks in the
"Report templates" section of `students.html`.
